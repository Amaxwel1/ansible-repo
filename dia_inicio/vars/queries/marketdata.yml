---
marketdata_queries:
  check_stream_loaded_paper:
    sql: |
      SELECT *
      FROM tb_bar_one_minute
      WHERE id_stock = (
        SELECT id_stock
        FROM tb_stock
        WHERE cd_stock = 'PETR4'
      )
      ORDER BY id_point DESC
      LIMIT 50;

---
# defaults file for must-gather-ansible

ocp_must_gather_clusters:
  tuti: "https://api.crc.testing:6443"
  hofix: "https://api.cluster-kjwhm.dynamic.redhatworkshops.io:6443"
  producao: "https://api.agcticosprocp.ocp.agorasenior.corp:6443"
  dr: "https://api.agalpcosdrocp.ocp.agorasenior.corp:6443"

ocp_must_gather_cluster: ""

ocp_must_gather_cifs_path: "//192.168.0.115/certs/Ansible/mail_attachments/"
ocp_must_gather_cifs_mount_dir: "/mnt/must-gather"
ocp_must_gather_cifs_domain: "" #"corp.bradesco.com.br"

ocp_must_gather_cifs_username: "{{ ansible_user }}"
ocp_must_gather_cifs_password: "{{ ansible_password }}"

ocp_must_gather_node: ""
ocp_must_gather_command: ""
ocp_must_gather_existing_case_id: ""

ocp_user: ""
ocp_password: ""

offline_token: 

rh_portal_user: 
ocp_must_gather_case_token: 
